; "===================================================================="
; " user.def:                                                       "
;
; "====================================================================="

	( define mailbox DIRECTOR_CAMSHOT_HIGH 98 )			; high priority camshot
	( define mailbox DIRECTOR_CAMSHOT_MED 99 )
	( define mailbox DIRECTOR_CAMSHOT_LOW 100 )    ; we really need to be able to type in equates into the mailbox fields

;	( define FALSE 0 )
	( define NULL 0 )
;	( define TRUE 1 )
